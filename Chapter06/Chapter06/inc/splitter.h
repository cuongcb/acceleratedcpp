#include <algorithm>
#include <cctype>
#include <iostream>
#include <string>
#include <vector>

using namespace std;

bool space(char c);
bool not_space(char c);
vector<string> split(const string &str);